To restore this backup:
1. Copy the files from `Saves` back into `%localappdata%\Hinterland\TheLongDark`. (You can paste
   that exact path into Windows Explorer.)
2. Copy the files from `ModData` back into `Mods/ModData`.
